import React from 'react';
import { Layout } from '../../Layout/';

export const Settings = () =>
{
    return(
        <Layout>
            <>
                <p>Settings page...</p>
            </>
        </Layout>
    );
};
